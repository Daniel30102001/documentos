//questao 19 completa

// Criação do array de frutas
let frutas = ['banana', 'maçã', 'laranja', 'manga', 'uva'];

// Usando o método includes para verificar se 'maçã' está presente no array
let temMaca = frutas.includes('maçã');

// Exibindo o resultado no console
console.log(temMaca);  // Retorna true se 'maçã' estiver no array, false caso contrário
