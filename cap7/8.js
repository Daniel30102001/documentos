//questao 8 completa

// Criação do array
let cores = ['vermelho', 'verde'];

// Usando o método unshift() para adicionar os elementos 'azul' e 'amarelo' no início
cores.unshift('azul', 'amarelo');

// Exibindo o array resultante
console.log(cores);
