//questao 9 completa

// Criação dos arrays
let frutas = ['maçã', 'banana'];
let maisFrutas = ['laranja', 'pera'];

// Usando o método concat() para combinar os dois arrays
let todasFrutas = frutas.concat(maisFrutas);

// Exibindo o array resultante
console.log(todasFrutas);
